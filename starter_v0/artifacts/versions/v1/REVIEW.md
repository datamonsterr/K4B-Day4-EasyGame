# v1 review

24/30 vs 20/30; zero provider errors. Missing-info failures reduced from 3 to 0 (hypothesis met). Regression: ['H20_format_without_refetch']. Six failures remain; all are missing structured tool calls. Some replies narrate actions without calling a tool.
